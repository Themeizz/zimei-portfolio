# 子美个人作品集（Zimei Portfolio）· 产品需求文档（PRD）

| 项目 | 内容 |
| --- | --- |
| 文档名称 | 子美个人作品集 PRD |
| 文档版本 | v1.0 |
| 更新日期 | 2026-08-12 |
| 文档状态 | 待评审 |
| 关联文档 | [docs/需求文档.md](docs/需求文档.md)（需求 v0.2） |

---

## 1. 产品概述与目标用户

### 1.1 产品简介

「子美个人作品集」是一个对外展示子美个人作品与经历的网站，兼具"个人名片"和"作品画廊"两种角色。访客可以快速了解子美是谁、做过什么、擅长什么，并方便地发起联系；子美本人则通过可视化后台（Sanity Studio）独立维护所有内容，无需编写代码。

### 1.2 一句话定位

**一个部署在 Vercel、后台用 Sanity 的暗黑科技感个人作品集：传图填表即可更新作品，换色板即可换风格。**

### 1.3 产品目标

1. **对外展示**：访客在 3 秒内了解子美的定位与代表作，并能顺畅浏览作品详情与联系方式。
2. **可编辑**：所有内容（作品、简介、社交链接、页脚）通过可视化后台维护，增加 / 删除 / 修改 / 排序 / 上下架均不涉及代码。
3. **可扩展**：主题、布局、页面模块可低成本调整；未来可新增博客、图库等板块。
4. **可发布**：部署于 Vercel，具备 HTTPS、自定义域名、SEO、移动端适配与自动部署能力。

### 1.4 非目标（当前版本明确不做）

- 不做博客板块（后续按需扩展）；
- 不做电商、支付、用户注册登录、评论区、多用户后台；
- 不做复杂动效和重交互（如 3D 场景、全屏动画），以性能和可维护性优先；
- 不做手机 App，仅做响应式网页。

### 1.5 目标用户与画像

| 用户类型 | 身份 | 使用场景 | 核心诉求 | 痛点 |
| --- | --- | --- | --- | --- |
| 访客 A | 潜在雇主 / 面试官 | 快速评估能力与作品质量 | 看到代表作、技术栈、代码链接 | 作品展示零散、找不到联系方式 |
| 访客 B | 潜在合作方 / 客户 | 判断是否值得合作 | 看到案例与成果，方便发起沟通 | 缺乏可信案例与明确 CTA |
| 访客 C | 同学 / 朋友 / 社区成员 | 浏览、转发、关注动态 | 体验流畅、内容有趣 | 加载慢、手机上体验差 |
| 维护者 | 子美本人（技术小白） | 日常更新作品与站点信息 | 不用写代码就能增删改内容 | 改内容要碰代码、流程复杂 |
| 未来维护者 | 半年 / 一年后的子美或协作者 | 长期维护与扩展 | 有文档、可快速上手、可迁移 | 记忆模糊、文档缺失、技术栈过时 |

### 1.6 核心用户故事

| 编号 | 角色 | 作为…… | 我想要…… | 以便…… |
| --- | --- | --- | --- | --- |
| US-1 | 访客 A | 打开首页 | 3 秒内看到"我是谁、做什么" | 决定是否继续浏览 |
| US-2 | 访客 A | 浏览作品 | 按卡片查看代表作品并进入详情 | 评估作品质量 |
| US-3 | 访客 A/B | 看完作品 | 找到邮箱 / 社交链接 | 发起联系 |
| US-4 | 子美 | 完成新作品 | 在后台填表、传封面、写描述并发布 | 网站自动更新上线 |
| US-5 | 子美 | 内容过时 | 修改 / 下架 / 删除 / 调整顺序 | 保持网站信息准确 |
| US-6 | 子美 | 想换风格 | 只改主题色板或布局配置 | 不重写代码也能焕然一新 |
| US-7 | 未来维护者 | 接手网站 | 阅读 README 快速上手 | 知道如何加作品、改样式、部署 |

---

## 2. 详细功能列表

### 2.1 功能清单总览

**模块 A：内容管理与可视化后台（Sanity CMS）**

| 编号 | 功能 | 说明 | 优先级 |
| --- | --- | --- | --- |
| FR-01 | 后台入口与登录 | 站点提供 /studio 后台入口，仅子美（Sanity 账号）可登录访问 | P0 |
| FR-02 | 站点信息管理 | 维护站点名称、一句话简介、个人简介、头像、邮箱、社交链接、导航、页脚 | P0 |
| FR-03 | 作品新增 | 后台创建新作品：类型、标题、封面、描述、标签、链接、日期等 | P0 |
| FR-04 | 作品编辑 | 后台修改已发布作品的任意字段，保存后前台更新 | P0 |
| FR-05 | 作品删除 | 删除作品，需确认；已发布作品删除后前台立即消失 | P0 |
| FR-06 | 作品复制 | 一键复制已有作品为草稿，便于快速新建相似作品 | P1 |
| FR-07 | 作品状态管理 | 状态机：草稿（draft）→ 已发布（published）→ 隐藏（hidden）；仅 published 在前台可见 | P0 |
| FR-08 | 作品排序与置顶 | 后台设置手动排序权重（order）与精选标记（featured）；置顶作品展示在首页 | P0 |
| FR-09 | 富文本块编辑 | 可视化块编辑器，支持标题、段落、列表、引用、代码块、图片、链接 | P1 |
| FR-10 | 图片管理 | 拖拽上传、CDN 托管、自动压缩 / 裁剪、可填 alt 文本 | P1 |
| FR-11 | 多媒体支持 | 作品可附加视频（URL / 文件）与 PDF 文档 | P1 |
| FR-12 | 内容预览 | 后台可预览作品在首页 / 详情页的呈现效果 | P1 |
| FR-13 | 版本历史 | 查看并恢复作品的历史版本（Sanity 原生能力） | P2 |

**模块 B：前台作品展示**

| 编号 | 功能 | 说明 | 优先级 |
| --- | --- | --- | --- |
| FR-14 | 首页 Hero | 展示姓名、一句话简介、技能标签、主 CTA（看作品 / 联系） | P0 |
| FR-15 | 置顶作品区块 | 首页展示 3–5 个精选作品卡片（featured=true） | P0 |
| FR-16 | 作品列表页 | /projects 卡片网格，按日期倒序展示全部已发布作品 | P0 |
| FR-17 | 标签筛选与搜索 | 按标签 / 类型筛选作品（列表页） | P1 |
| FR-18 | 作品详情页 | /projects/:slug，展示封面、富文本正文、图集、链接、技术栈、日期 | P0 |
| FR-19 | 空状态 | 无作品 / 无匹配结果时展示友好提示与引导 | P1 |
| FR-20 | 404 页面 | 统一风格的 404，提供返回首页入口 | P0 |
| FR-21 | 分页 / 加载更多 | 作品数量较多时的分页或"加载更多" | P2 |

**模块 C：个人品牌与联系**

| 编号 | 功能 | 说明 | 优先级 |
| --- | --- | --- | --- |
| FR-22 | 时间线 | 经历 / 里程碑按时间倒序展示（工作、教育、事件） | P0 |
| FR-23 | 关于我 | 个人介绍、技能清单、教育 / 经历详情 | P1 |
| FR-24 | 联系区块 | 邮箱 + 社交链接；后续可扩展联系表单 | P0 |
| FR-25 | 简历页 | 独立 /resume 页面，方便投递时附链接（可选） | P2 |
| FR-26 | 页脚 | 版权信息、导航链接、社交链接、统计声明（如启用） | P0 |

**模块 D：平台、部署与数据**

| 编号 | 功能 | 说明 | 优先级 |
| --- | --- | --- | --- |
| FR-27 | 自动部署 | 连接 GitHub 仓库与 Vercel，push 后自动构建发布；支持预览部署 | P0 |
| FR-28 | 自定义域名 | 绑定自定义域名并自动配置 HTTPS（先用 vercel.app 免费域名） | P1 |
| FR-29 | SEO | sitemap、robots、语义化 HTML、OG / Twitter 卡片、结构化数据 | P1 |
| FR-30 | 访问统计 | 轻量匿名统计（Umami / Plausible 可选） | P2 |
| FR-31 | 数据备份 | 定期导出 Sanity 内容；代码全部在 Git 仓库可回溯 | P1 |
| FR-32 | README 与操作文档 | 说明如何新增作品、修改样式、部署、登录后台 | P0 |
| FR-33 | 主题配置 | 颜色 / 字体通过 CSS 变量（设计令牌）配置，改色板即换风格 | P0 |
| FR-34 | 国际化预留 | i18n 结构预留，当前仅中文，后续可加英文 | P2 |
| FR-35 | 浅色模式 | 浅色主题切换（预留设计令牌，默认深色） | P2 |
| FR-36 | 联系表单 | 可提交的表单（需防垃圾方案，如 honeypot / 验证码） | P2 |

### 2.2 关键功能详细规格

#### FR-03 / FR-04 作品新增与编辑

**表单字段与校验：**

| 字段 | 类型 | 必填 | 校验规则 |
| --- | --- | --- | --- |
| slug | string | 是 | 由标题自动生成，可手动修改；唯一；仅小写字母、数字、连字符 |
| type | enum | 是 | code / design / writing / other |
| title | string | 是 | 1–100 字符 |
| subtitle | string | 否 | 1–200 字符 |
| summary | string | 是 | 1–300 字符（卡片展示） |
| cover | image | 是 | 建议 16:10；需填 alt 文本 |
| gallery | image[] | 否 | 每张图需 alt 文本 |
| video / pdf | file/url | 否 | 视频支持 URL 或上传；PDF 限制 ≤ 20MB |
| description | rich text | 是 | 块编辑器；允许标题、段落、列表、引用、代码块、图片、链接 |
| tags / tech | string[] | 否 | 单条 ≤ 20 字符 |
| links | object | 否 | demo、github、article 三类链接，需为合法 URL |
| date | date | 是 | 不可晚于当前日期 + 1 年（防误填） |
| featured | boolean | 否 | 默认 false |
| status | enum | 否 | draft / published / hidden，默认 draft |
| order | number | 否 | 数值越小越靠前 |

**验收标准：**

- 后台保存草稿后，前台不出现该作品；
- 将状态改为 published 后，前台列表与首页（若 featured）在更新后可见；
- slug 冲突时后台给出明确错误提示并禁止保存。

#### FR-07 作品状态机

```
draft ──发布──▶ published ──隐藏──▶ hidden
  ▲               │                  │
  └───────继续编辑 ◀──── 编辑 ◀───────┘
```

- 只有 `published` 状态的作品出现在前台；
- `hidden` 用于暂时下线但不删除；
- 任何状态下都可以编辑；编辑已发布作品不改变其状态。

#### FR-08 排序规则

1. 列表页默认按 `order` 升序，其次按 `date` 倒序；
2. 首页置顶作品取 `featured=true` 且 `published` 的前 3–5 个；
3. 后台修改 order / featured 后，前台按最新配置重新排序。

#### FR-16 作品列表页

- 默认展示全部已发布作品，按上述排序规则；
- 卡片展示：封面、标题、一句话描述（summary）、标签、日期；
- 点击卡片进入详情页；整卡可点击，链接语义正确（不使用无意义的包裹 div）。

#### FR-18 作品详情页（按类型差异化呈现）

| 类型 | 详情页侧重 |
| --- | --- |
| code（编程） | 技术栈、Demo / GitHub 链接、项目简介、图集 / 截图 |
| design（设计） | 大图图集优先、设计说明、过程稿（可选） |
| writing（写作） | 正文排版优先，标题层级清晰、引用 / 代码块样式完善 |
| other | 通用布局：封面 + 正文 + 链接 |

#### FR-29 SEO 规则

- 每个作品可单独设置 SEO 标题、描述、OG 图；未设置时使用默认规则：`{作品名} - 子美` + 作品摘要；
- 所有页面输出唯一 `<title>` 与 meta description；
- 自动生成 sitemap.xml 与 robots.txt；
- 首页输出 Person 结构化数据，作品页输出 CreativeWork / Project 结构化数据。

#### FR-33 主题配置

- 颜色、字体、圆角、间距等全部收敛为 CSS 变量（设计令牌）；
- 换风格 = 改一组变量，不涉及组件代码；
- 预留浅色模式所需变量（P2 实现切换）。

---

## 3. 功能优先级与版本规划

### 3.1 优先级定义

- **P0（MVP v1.0）**：没有这些功能产品无法成立，必须随首版上线；
- **P1（v1.1）**：提升完整度和体验，首版后尽快补齐；
- **P2（v2.0）**：按需演进的扩展能力。

### 3.2 优先级清单

| 优先级 | 功能编号 |
| --- | --- |
| P0（MVP） | FR-01、02、03、04、05、07、08、14、15、16、18、20、22、24、26、27、32、33 |
| P1（v1.1） | FR-06、09、10、11、12、17、19、23、28、29、31 |
| P2（v2.0） | FR-13、21、25、30、34、35、36 |

### 3.3 版本路线图

| 里程碑 | 目标 | 包含范围 | 验收 |
| --- | --- | --- | --- |
| M1 · MVP v1.0 | 先上线，跑通"后台编辑 → 自动上线"全流程 | 作品管理全流程、首页（Hero + 置顶作品）、作品列表与详情、联系、时间线、页脚、Vercel 自动部署、README | A1–A5、A11 |
| M2 · v1.1 | 像一个成熟的作品集 | 关于我、标签筛选、富文本增强、图片 / 多媒体管理、SEO 全套、自定义域名、数据备份、内容预览 | A6–A9 |
| M3 · v2.0 | 按需演进 | 简历页、联系表单、浅色模式、国际化、版本历史、访问统计、新板块（博客 / 图库） | A10 及新增项 |

---

## 4. 界面设计要求

### 4.1 设计风格：暗黑科技感

关键词：**深色背景、霓虹强调色、发光与网格、玻璃拟态、克制动效、等宽字体点缀**。

设计原则：

- 深色为主，信息层次靠背景层级和文字明度区分，不依赖大面积色块；
- 强调色克制使用（按钮、链接、焦点、标签、数据高亮），单屏不超过两个主强调色；
- 动效服务信息传达，不做无意义动画；
- 内容优先，任何装饰不得影响可读性。

### 4.2 设计令牌（建议色板）

| 令牌 | 色值 | 用途 |
| --- | --- | --- |
| `--bg-base` | `#05070D` | 页面最底层背景 |
| `--bg-surface` | `#0B101C` | 卡片 / 区块背景 |
| `--bg-elevated` | `#121A2B` | 悬浮层、导航背景 |
| `--text-primary` | `#E6EDF3` | 主要文字 |
| `--text-secondary` | `#8B98A9` | 次要文字、描述 |
| `--text-muted` | `#5B6B82` | 辅助信息、时间戳 |
| `--accent-cyan` | `#22D3EE` | 主强调色（链接、CTA、焦点） |
| `--accent-violet` | `#8B5CF6` | 辅助强调色（标签、渐变） |
| `--accent-green` | `#10B981` | 成功 / 在线状态（可选） |
| `--border` | `rgba(148,163,184,0.16)` | 分隔线、卡片描边 |
| `--glow` | 强调色 20–30% 透明度的光晕 | 卡片 hover、按钮发光 |

对比度要求：正文与背景 ≥ 4.5:1，大号文字 ≥ 3:1，目标尽量满足 WCAG AA。

### 4.3 字体与排版

- 标题：Space Grotesk（拉丁） + Noto Sans SC（中文）；
- 正文：Inter + Noto Sans SC；
- 代码 / 标签点缀：JetBrains Mono；
- 字号阶梯：12 / 14 / 16 / 18 / 24 / 32 / 48 / 64px，行高 1.5–1.6，标题 1.15–1.25；
- 正文每行 45–75 字符，移动端不小于 16px。

### 4.4 组件与布局

**组件清单：** 导航栏、Hero、技能标签、作品卡片、作品详情、时间线、关于区块、联系 CTA、页脚、404。

**首页布局（MVP）：**

```
┌──────────────────────────────────────┐
│ 导航：logo · 作品 · 时间线 · 联系       │
├──────────────────────────────────────┤
│ Hero：姓名 / 一句话简介 / 技能标签       │
│       [看作品] [联系我]                │
├──────────────────────────────────────┤
│ 置顶作品（3–5 张卡片）                  │
├──────────────────────────────────────┤
│ 精选时间线（倒序 3–5 条）+ 查看全部      │
├──────────────────────────────────────┤
│ 联系 CTA：邮箱 / 社交链接               │
│ 页脚：版权 · 导航 · 统计声明             │
└──────────────────────────────────────┘
```

**作品卡片规格：**

- 封面比例 16:10，统一裁剪；
- hover：描边变亮 + 轻微发光 + 卡片上浮 2–4px；
- 响应式列数：手机 1 列、平板 2 列、桌面 3 列；
- 卡片信息：封面、标题、一句话描述、标签、日期。

### 4.5 动效规范

- 入场：fade + 上移（8–16px），时长 200–400ms，交错出现 ≤ 100ms；
- hover：发光边框、透明度变化，过渡 150–250ms；
- 滚动：首屏以下区块可使用 IntersectionObserver 渐显，禁用滚动绑架；
- `prefers-reduced-motion: reduce` 时禁用所有非必要动画；
- 不引入重型动画库，首选 CSS 实现。

### 4.6 响应式断点

| 断点 | 宽度 | 布局行为 |
| --- | --- | --- |
| 手机 | < 640px | 单列卡片、汉堡菜单、正文 16px |
| 平板 | 640–1024px | 两列卡片、简化导航 |
| 桌面 | > 1024px | 三列卡片、完整导航、版心最大约 1200px |

### 4.7 无障碍要求

- 语义化标签（header / nav / main / article / footer）；
- 键盘可完整操作（焦点可见、跳转自然、无焦点陷阱）；
- 所有图片提供 alt；装饰性图片使用空 alt；
- 交互元素有明确焦点样式（强调色描边）；
- 点击目标最小 44×44px（移动端）；
- 颜色不作为唯一信息载体（如状态同时用文字表达）。

---

## 5. 技术栈建议

### 5.1 推荐技术栈

| 层级 | 技术 | 选择理由 |
| --- | --- | --- |
| 框架 | Next.js（App Router）+ TypeScript | Vercel 原生支持；生态成熟；SSG / ISR 兼顾性能与更新时效 |
| CMS / 后台 | Sanity（Sanity Studio） | 可视化表单式编辑、图片 CDN、版本历史、免费额度足够 |
| 样式 | Tailwind CSS + CSS 变量 | 开发高效；设计令牌天然契合"换肤"需求 |
| 图片 | Sanity CDN + next/image | 自动压缩、格式转换、懒加载 |
| 部署 | Vercel（GitHub 仓库连接） | 一键部署、自动 HTTPS、预览部署、免费 Hobby 版 |
| 版本管理 | Git + GitHub | 代码可回溯、可协作、可迁移 |
| 统计（可选） | Umami / Plausible | 轻量、匿名、无 Cookie 弹窗负担 |

### 5.2 架构与数据流

```
┌────────────────────────────────────────────┐
│  Sanity CMS（可视化后台 /studio）            │
│  子美编辑作品、站点信息、传图                 │
└──────────────────┬─────────────────────────┘
                   │ 内容数据（SDK / API）
┌──────────────────▼─────────────────────────┐
│  Next.js（SSG / ISR + 按需重新验证）          │
│  渲染首页、列表、详情、关于、时间线等页面       │
└──────────────────┬─────────────────────────┘
                   │ 静态产物 / 增量生成
┌──────────────────▼─────────────────────────┐
│  Vercel（CDN + HTTPS + 自动部署）            │
│  访客访问，图片由 Sanity CDN 提供            │
└────────────────────────────────────────────┘
```

**更新生效策略：**

- 站点以静态产物为主（SSG），保证加载速度；
- Sanity 内容变更后触发重新验证（ISR revalidate 或 Vercel webhook 重建）；
- 目标：后台保存后 1–5 分钟内前台更新可见。

### 5.3 内容数据模型（Sanity Schema）

**`site`（单例）：** name、headline、bio、avatar、email、social（github 等）、nav[]、footer、theme。

**`project`：**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| slug | string | 是 | 唯一，URL 标识 |
| type | enum | 是 | code / design / writing / other |
| title / subtitle / summary | string | title、summary 必填 | 文案字段 |
| cover | image | 是 | 封面图（含 alt） |
| gallery | image[] | 否 | 图集 |
| video | file / url | 否 | 视频演示 |
| pdf | file | 否 | PDF 文档 |
| description | rich text | 是 | 详情正文 |
| tags / tech | string[] | 否 | 标签 / 技术栈 |
| links | object | 否 | demo / github / article |
| date | date | 是 | 时间排序依据 |
| featured | boolean | 否 | 置顶精选 |
| status | enum | 否 | draft / published / hidden |
| order | number | 否 | 手动排序 |

**`timelineEntry`：** date、title、org、type（work / education / event）、description、links。

### 5.4 备选方案

| 场景 | 备选 | 说明 |
| --- | --- | --- |
| 想更轻量、后台能力要求低 | Astro + Decap CMS | 内容存 Git 仓库、无第三方托管依赖；编辑体验不如 Sanity |
| 未来需要复杂动态功能（如登录、社交） | Next.js + 自建 API | 可在现有架构上扩展，不推荐现在做 |

### 5.5 维护性说明（对技术小白）

- 改内容：登录 /studio 填表即可，不动代码；
- 改样式：改一组 CSS 变量（换色板）；
- 改结构：由开发者按 README 指引增删组件 / 页面；
- 数据不丢：代码在 Git，内容在 Sanity 云端并可导出。

---

## 6. 非功能性需求

### 6.1 性能

| 编号 | 指标 | 目标 |
| --- | --- | --- |
| NFR-P1 | 首屏加载 | LCP < 2.5s（移动端 4G 模拟） |
| NFR-P2 | 布局稳定性 | CLS < 0.1 |
| NFR-P3 | Lighthouse | 性能 ≥ 90（移动端） |
| NFR-P4 | 图片 | 懒加载 + 响应式尺寸 + 现代格式（AVIF / WebP） |
| NFR-P5 | JS 体积 | 首页核心 JS 压缩后 < 200KB（gzip） |
| NFR-P6 | 缓存 | 静态资源长缓存；页面按内容变更失效 |

### 6.2 安全

| 编号 | 要求 |
| --- | --- |
| NFR-S1 | 全站 HTTPS（Vercel 自动配置） |
| NFR-S2 | 后台仅 Sanity 认证用户可访问；不暴露写接口给访客 |
| NFR-S3 | 环境变量（Sanity token 等）只存 Vercel / 本地 .env，绝不提交仓库 |
| NFR-S4 | 内容渲染防 XSS：React 默认转义 + Sanity Portable Text 渲染白名单 |
| NFR-S5 | next/image 只允许配置的图片域名，防开放代理滥用 |
| NFR-S6 | 依赖定期升级，修复已知漏洞；部署前跑 `npm audit` 检查 |
| NFR-S7 | 若做联系表单：加 honeypot / 限流防垃圾提交 |

### 6.3 隐私与合规

| 编号 | 要求 |
| --- | --- |
| NFR-V1 | 不采集访客个人数据，不设置第三方追踪 Cookie |
| NFR-V2 | 若启用统计，使用匿名方案（Umami / Plausible），并在页脚声明 |
| NFR-V3 | 内容导出能力保留，满足"数据可带走" |

### 6.4 可靠性、可维护性与可扩展性

| 编号 | 类别 | 要求 |
| --- | --- | --- |
| NFR-R1 | 可靠性 | 个人站目标月可用性 ≥ 99%（Vercel 托管自带）；页面渲染失败有兜底错误提示 |
| NFR-R2 | 可维护性 | 目录结构清晰；TypeScript 类型完整；README 含内容 / 样式 / 部署三份指南 |
| NFR-R3 | 可扩展性 | 新增页面 = 新增路由 + 可选新 Schema；主题换肤不改组件 |
| NFR-R4 | 数据备份 | Sanity 定期导出（至少每月或每次大改动）；Git 历史可回滚 |
| NFR-R5 | 兼容性 | Chrome / Edge / Firefox / Safari 最近两个大版本；移动端体验完整 |
| NFR-R6 | 无障碍 | 目标 WCAG AA（对比度、键盘、语义、减少动效） |
| NFR-R7 | SEO | sitemap / robots / OG / 结构化数据 / canonical；移动端优先索引 |
| NFR-R8 | 监控（可选） | Vercel Analytics 观察性能；Sanity 用量监控避免超配额 |

### 6.5 内容交付

| 编号 | 要求 |
| --- | --- |
| NFR-C1 | 图片经 CDN 分发，全球访问延迟可控 |
| NFR-C2 | 页面静态化为主，后台更新后 1–5 分钟内前台生效 |
| NFR-C3 | 无作品 / 空数据时不出现布局错乱或白屏 |

---

## 7. 全局验收标准

| 编号 | 验收点 |
| --- | --- |
| A1 | 首页 3 秒内能看出"是谁 + 做什么"，包含姓名与一句话简介 |
| A2 | 不写代码，在后台新增一个作品（含封面、描述、链接），保存发布后网站自动更新 |
| A3 | 后台可修改作品内容、封面、排序、上下架，发布后界面正确渲染 |
| A4 | 作品详情页排版正确（标题 / 列表 / 代码块 / 图片 / 链接），不同类型作品呈现合理 |
| A5 | 手机端浏览全部页面无横向滚动、可读可用 |
| A6 | 修改主题色板 / 字体后全站风格一致变化 |
| A7 | 新增页面只需添加路由文件，页面内容可通过后台维护 |
| A8 | Lighthouse 性能 ≥ 90（移动端模拟） |
| A9 | 每个页面有唯一 title / description，分享时正确显示标题和封面 |
| A10 | README 清楚说明如何新增作品、改样式、部署；后台入口与登录方式写明 |
| A11 | 后台可修改站点简介、社交链接、页脚，发布后生效 |
| A12 | 后台保存后，前台在 5 分钟内看到内容更新（自动部署 / 重新验证） |

---

## 8. 里程碑与交付计划

| 阶段 | 周期建议 | 交付物 | 退出条件 |
| --- | --- | --- | --- |
| M1 · MVP v1.0 | 第 1–2 周 | 项目骨架、Sanity 接入、首页 / 列表 / 详情 / 联系 / 时间线、Vercel 部署、README | A1–A5、A11、A12 通过 |
| M2 · v1.1 | 第 3–4 周 | 关于我、筛选、SEO、图片 / 多媒体、自定义域名、备份 | A6–A9 通过 |
| M3 · v2.0 | 按需 | 简历页、表单、浅色模式、国际化、新板块 | 按新增功能验收 |

---

## 9. 依赖、风险与开放问题

### 9.1 外部依赖

- GitHub 账号（部署与代码托管；当前 CLI 登录令牌已失效，需重新 `gh auth login`）；
- Sanity 账号（可用 GitHub / Google 登录创建项目）；
- Vercel 账号（可用 GitHub 登录并导入仓库）。

### 9.2 风险与对策

| 风险 | 影响 | 对策 |
| --- | --- | --- |
| Sanity 免费额度变化 / 服务不可用 | 后台与内容不可用 | 定期导出备份；内容可迁移到其他 CMS |
| 图片素材缺失或过大 | 上线延期、性能差 | 先用手头素材 + CDN 压缩；统一命名规范 |
| 技术小白操作失误（误删作品） | 内容丢失 | 删除需确认 + 版本历史 + 导出备份 |
| 风格与参考站雷同 | 辨识度低 | 自定义配色、文案、动效，避免直接复用 |
| 长期无人维护导致技术过时 | 升级成本高 | 内容与代码分离；依赖锁定 + 定期升级 |

### 9.3 开放问题（需产品负责人确认）

1. 自定义域名：有 / 无（无则先用 `xxx.vercel.app`）；
2. 联系方式：仅邮箱 + 社交链接，还是需要联系表单；
3. 作品素材：作品数量、是否有现成封面图 / 截图；
4. 简历页：是否需要独立 URL；
5. 语言：仅中文，还是中英双语；
6. GitHub 仓库：公开（对外展示）还是私有，仓库名是否用 `zimei-portfolio`。

---

## 10. 术语表

| 术语 | 说明 |
| --- | --- |
| CMS | 内容管理系统，本项目使用 Sanity |
| SSG / ISR | 静态站点生成 / 增量静态再生，Next.js 的渲染策略 |
| OG 图 | 社交分享时展示的标题、描述与封面 |
| 设计令牌 | 以 CSS 变量定义的颜色、字体、间距等设计规范 |
| CTA | 引导访客行动的按钮 / 链接（如"看作品""联系我"） |

---

## 附：一句话总结

**一个"内容与代码分离"的暗黑科技感作品集**：访客看得到你的故事和作品，你通过后台随手更新内容，开发者能低成本换肤和扩展——部署在 Vercel，数据在 Sanity，代码在 GitHub。
